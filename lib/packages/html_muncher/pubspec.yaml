name: html_muncher
description: Munch html nodes to flutter widgets
version: 0.0.1
homepage: https://github.com/realth000/tsdm_client/lib/packages/html_muncher
publish_to: none

environment:
  # sdk: '>=3.1.3 <4.0.0'
  # Only requires full null safety.
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  surf_lint_rules: ^3.0.0
  universal_html: ^2.2.4

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
